services:
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    image: http-rpc/nginx-frontend:local
    volumes: !override
      - ./deploy/nginx/nginx.frontend.conf:/etc/nginx/nginx.conf:ro
      - nginx_certs:/certs

volumes:
  nginx_certs:
    driver: local
