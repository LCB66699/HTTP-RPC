name: CI C++ Sheet

on:
  pull_request:
    paths:
      - 'server/src/sheet/**'
      - 'server/src/shared/**'
      - 'server/include/sheet/**'
      - 'server/include/shared/**'
      - 'proto/rpc_spreadsheet.proto'
      - 'proto/rpc_common.proto'
      - 'CMakeLists.txt'
      - 'Dockerfile'
  workflow_dispatch:

jobs:
  sheet-ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build builder image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: builder
          build-args: SERVICE=sheet
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true
          push: false
          tags: cpp-builder:latest

      - name: Run C++ unit tests
        run: |
          docker run --rm cpp-builder:latest sh -c "
            cd /src &&
            cmake --build build --target cpp_test -j\$(nproc) &&
            ./build/cpp_test
          "

      - name: Build sheet runtime image (compilation check)
        uses: docker/build-push-action@v6
        with:
          context: .
          target: runtime
          build-args: SERVICE=sheet
          cache-from: type=gha
          cache-to: type=gha,mode=max
          load: true
          push: false
