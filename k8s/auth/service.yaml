apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: http-rpc
spec:
  clusterIP: None   # Headless — gRPC round_robin 直接见 Pod IP
  selector:
    app: auth
  ports:
    - port: 50051
      name: grpc
